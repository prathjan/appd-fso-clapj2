{
    "name": "${solution}_metric_mapping_for_vm_count",
    "displayName": "Intersight Metric mapping for vm_count",
    "namespace": {
        "name": "${solution}",
        "version": 1
    },
    "kind": "metricMapping",
    "scopeFilter": "entity.namespace.equals('${solution}') && entity.type.equals('account')",
    "nameMappings": {
        "intersight.vm.count": "vm_count"
    }
}