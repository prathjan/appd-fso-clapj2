{
    "kind": "template",
    "name": "dashui:ecpDetails",
    "target": "${solution}:hyperflex_cluster",
    "view": "default",
    "element": {
        "instanceOf": "ocpSingle",
        "elements": [
            {
                "instanceOf": "${solution}:hyperflex_clusterDetailsList"
            }
        ],
        "nameAttribute": "${solution}.hyperflex.device.id"
    }
}
